Ticket 002 validation log
Date: 2026-08-13 Europe/Warsaw
Branch: ticket/002-commercial-models
Accepted base: a9b51e5106af126316045a98362dd8d43d553ff3
Implementation commit: 9592e47

PASS: Draft 2020-12 contract
- `Draft202012Validator.check_schema` accepted the closed schema.
- Offer, request, lifecycle-state and receipt examples all validate against the
  metaschema-checked document family.
- The offer example covers a free trial, Basic and Pro with monthly/yearly
  prices, PrePaid, mixed PLN/EUR settlements and a perpetual On-Premise licence
  with optional annual maintenance.

PASS: dependency-free host conformance
- `python3 standard/conformance.py --all` returned `ok: true`.
- Positive document variants: 4.
- Independently rejected adversarial cases: 32.
- Rejections cover false capability parity, recurring or non-expiring PrePaid,
  incompatible/mismatched add-ons, duplicate intervals/prices, Cloud perpetual
  licensing, incomplete/ambiguous display conversion, implicit price choice,
  browser-issued/overdrawn/expired/duplicate usage grants and the original
  payment, webhook, trial, provisioning and receipt safety boundaries.
- Schema digest:
  `sha256:659e2d0124b236e86f76786c37bc22bbd326f274eb6b9ca9c3a20d82e33f26cd`.
- Grammar digest:
  `sha256:2565b1c0e93c1f5ee84c299428ef8592e295b4a3337585409ae1ee19644d7690`.

FIXED: adversarial test independence
- The v1 runner's lambdas previously captured the mutable `bad` name late, so
  they all received the last constructed document at execution time.
- The new `add` helper binds validator and document as lambda defaults. Every
  named case now exercises its intended mutation before it is counted.

PASS: networkless Docker conformance
- An initial plain `docker compose run --rm conformance` correctly exposed that
  Compose reused an earlier local image with the prior schema digest.
- `docker compose run --rm --build conformance` rebuilt from exact source and
  returned the same 32 rejections and exact host schema/grammar digests.
- The service remains networkless, read-only, capability-dropped and
  no-new-privileges under the pinned Python image.

PASS: static and governance gates
- `python3 -m py_compile standard/conformance.py` passed.
- `git diff --check` passed.
- `./project/governance-check.sh --actor agent` returned
  `GOV-PASS: passed (0 errors, 0 warnings)`.
- Exactly five implementation files changed across the two approved components;
  no dependency, secret, deployment or external publication change occurred.

Publication authorization
- The owner follow-up “a powinny być” authorizes pushing the exact ticket
  branch, opening its pull request and pursuing protected merge into `main`.
- Exact-head trusted review, repository/PR/ticket/actor bindings and the remote
  lifecycle gate remain mandatory; no direct or bypass merge is authorized.

Protected review and integration
- Pull request: https://github.com/wellmanifest/saas-lifecycle/pull/2
- The required remote lifecycle check passed for exact HEAD
  `73cd6ec175117b0749a05dbf6421d7e39c88af95`.
- External Validator workflow run `31732066238` validated the repository, pull
  request, ticket, actor and exact-head bindings.
- `ifuri-validator-agent[bot]` approved the exact HEAD with deterministic
  decision `D-002-7180`. Its optional GLM 5.2 advisory was unavailable because
  the model returned invalid JSON; that advisory was non-authoritative.
- The Validator merged pull request #2 at 2026-08-13T18:44:16Z as
  `88eb060e5a765913958612e128dbd444b37665d6`.
- GitHub deleted the merged remote branch. The linked implementation worktree
  was clean, its HEAD was reachable from `origin/main`, and it was removed and
  pruned before this governance-only closure.

Terminal workspace audit
- Both released local ticket branches were confirmed reachable from `main` and
  deleted. `saas-lifecycle` has one registered checkout on `main` and no local
  or remote ticket branch.
- The first Goal workspace audit identified five clean linked worktrees owned
  by separate active `new-project`, `poa` and `dsl` tickets. They were preserved
  rather than treated as disposable data.
- The audit was repeated with those five exact active checkout paths explicitly
  allowlisted. `goal governance workspace-check` returned
  `GOV-WORKSPACE-PASS: passed (0 errors, 0 warnings)` for the scoped terminal
  audit.
