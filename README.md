# Wellmanifest SaaS Lifecycle

Experimental, payment-provider-neutral standard for signup, trial,
subscription, plan change, tenant provisioning and auditable receipts.

The normative artifacts live in `standard/`; architecture and lifecycle
guidance live in `docs/`. This repository never processes payments, stores
payment credentials or deploys customer workloads.
